<header class="site-header">
  <div class="container header-inner">
    <a class="brand" href="/" aria-label="Amhiland, accueil">
      <span class="brand-mark" aria-hidden="true">A</span>
      <span>Amhiland</span>
    </a>

    <nav class="desktop-nav" aria-label="Navigation principale">
      <a href="/#actualites">Actualités</a>
      <a href="/#quiz">Quiz</a>
      <a href="/#tests">Tests</a>
      <a href="/#rubriques">Découvrir</a>
      <a href="/a-propos/">À propos</a>
    </nav>

    <details class="mobile-nav">
      <summary aria-label="Ouvrir la navigation">Menu <span aria-hidden="true">+</span></summary>
      <nav aria-label="Navigation principale">
        <a href="/#actualites">Actualités</a>
        <a href="/#quiz">Quiz</a>
        <a href="/#tests">Tests</a>
        <a href="/#rubriques">Découvrir</a>
        <a href="/a-propos/">À propos</a>
      </nav>
    </details>
  </div>
</header>

<style>
  .site-header {
    position: relative;
    z-index: 5;
    border-bottom: 1px solid var(--color-line);
    background: var(--color-paper);
  }

  .header-inner {
    display: flex;
    min-height: 5.25rem;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .brand {
    display: inline-flex;
    flex: 0 0 auto;
    align-items: center;
    gap: 0.6rem;
    color: var(--color-ink);
    font-size: 1.05rem;
    font-weight: 750;
    letter-spacing: -0.045em;
    text-decoration: none;
  }

  .brand-mark {
    display: grid;
    width: 1.7rem;
    aspect-ratio: 1;
    place-items: center;
    border: 1px solid currentColor;
    border-radius: 50%;
    color: var(--color-accent);
    font-family: var(--font-display);
    font-size: 1rem;
    font-style: italic;
    font-weight: 500;
  }

  .desktop-nav,
  .mobile-nav nav {
    display: flex;
    align-items: center;
    gap: clamp(1rem, 2.4vw, 2.2rem);
  }

  .desktop-nav a,
  .mobile-nav nav a {
    color: var(--color-muted);
    font-size: 0.82rem;
    font-weight: 600;
    text-decoration: none;
    transition: color var(--transition-quick);
  }

  .desktop-nav a:hover,
  .mobile-nav nav a:hover {
    color: var(--color-accent);
  }

  .mobile-nav {
    display: none;
  }

  @media (max-width: 52rem) {
    .header-inner {
      min-height: 4.5rem;
    }

    .desktop-nav {
      display: none;
    }

    .mobile-nav {
      position: relative;
      display: block;
    }

    .mobile-nav summary {
      display: flex;
      min-height: 2.75rem;
      align-items: center;
      gap: 0.65rem;
      cursor: pointer;
      color: var(--color-ink);
      font-size: 0.85rem;
      font-weight: 650;
      list-style: none;
    }

    .mobile-nav summary::-webkit-details-marker {
      display: none;
    }

    .mobile-nav summary span {
      color: var(--color-accent);
      font-size: 1.2rem;
      transition: transform var(--transition-quick);
    }

    .mobile-nav[open] summary span {
      transform: rotate(45deg);
    }

    .mobile-nav nav {
      position: absolute;
      top: calc(100% + 0.9rem);
      right: 0;
      display: grid;
      min-width: 12rem;
      gap: 0;
      padding: 0.45rem;
      border: 1px solid var(--color-line);
      background: var(--color-paper);
      box-shadow: 0 1rem 2rem rgb(36 37 31 / 10%);
    }

    .mobile-nav nav a {
      padding: 0.7rem 0.8rem;
    }
  }
</style>
